import { DatetimeSplitPipe } from './datetime-split.pipe';

describe('DatetimeSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
