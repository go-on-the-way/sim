import { DropdownDataPipe } from './dropdown-data.pipe';

describe('DropdownDataPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownDataPipe();
    expect(pipe).toBeTruthy();
  });
});
